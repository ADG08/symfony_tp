{% extends 'base.html.twig' %}

{% block title %}New Reaction{% endblock %}

{% block body %}
    <div class="flex flex-col gap-2">
        <h1>Create new Reaction</h1>

        <div class="text-black p-2 bg-indigo-900">
            {{ include('reaction/_form.html.twig') }}
        </div>
        <a href="{{ path('app_reaction_index') }}">back to list</a>
    </div>
{% endblock %}
