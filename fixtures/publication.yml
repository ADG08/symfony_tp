App\Entity\Publication:
    publication_{1..20}:
        title: "<sentence()>"
        content: "<paragraph()>"
        createdAt: "<dateTimeBetween('-100 days', 'now')>"
        publisher: "@user_*"
        tags: ["@tag_*", "@tag_*"]